import { InterestPoint } from "./interest-points";

export function dijkstra(interestPoint: InterestPoint, terrain: Uint8Array): Float32Array {
	const navigationField = new Float32Array;



	return navigationField;
}